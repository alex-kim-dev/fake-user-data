export enum Locale {
  en = 'en',
  es = 'es',
  fr = 'fr',
  default = Locale.en,
}

export const enum SEED {
  MIN = 0,
  MAX = 9_999_999,
}

export const enum ERRORS {
  DEFAULT = 0,
  MIN = 0,
  MAX = 10_000,
}

export const enum PAGE {
  DEFAULT = 0,
  USERS = 20,
}
